services:
  app:
    build: .
    container_name: new-project-app
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: new-project-redis
    ports:
      - "6379:6379"